import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SongActionScreen = () => {
	return (
		<View>
			<Text>SongActionScreen</Text>
		</View>
	);
};

export default SongActionScreen;

const styles = StyleSheet.create({});
